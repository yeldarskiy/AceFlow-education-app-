# ── Database ──────────────────────────────────────────────────────────────
db.url=jdbc:mysql://localhost:3306/aceflow?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
db.username=root
db.password=
db.driver=com.mysql.cj.jdbc.Driver

# ── Connection Pool ────────────────────────────────────────────────────────
pool.size=10
pool.timeout=30000

# ── Application ───────────────────────────────────────────────────────────
app.name=AceFlow
app.version=1.0.0
app.default.language=en
app.session.timeout=1800

# ── XP & Gamification ─────────────────────────────────────────────────────
xp.per.test.question=10
xp.per.flashcard.mastered=5
xp.per.goal.completed=100
xp.streak.bonus=50
